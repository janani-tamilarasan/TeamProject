console.log(12)
alert(2)
$('.member-selection').empty();
$('.member-selection').append( $('
%option> Select the Member
'));
- @members.each do |member|
  $('.member-selection').append($('
  %option{:value => "#{member.id}"}>= member.name
  '));
